groups:
  - name: request_rate
    rules:
      - alert: HighRequestRate
        expr: rate(requests_total[2m]) > 15
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "High request rate detected"
          description: "There have been more than 15 requests in the last 2 minutes."
